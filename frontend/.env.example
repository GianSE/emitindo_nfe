# URL do backend
VITE_API_URL=http://localhost:3001

# OBS: o SSO é configurado DENTRO do ERP (menu Configurações), então o frontend
# descobre o issuer/client_id em runtime via GET /config/sso — nada aqui.
